# Připojení k PostgreSQL (databáze fakturaApp na NAS)
PG_HOST=192.168.1.50
PG_PORT=5432
PG_DATABASE=fakturaApp
PG_USER=postgres
PG_PASSWORD=tvoje_heslo

# true = ukládat do PostgreSQL místo JSON souborů (výchozí: false / chybí = JSON)
# CONNECTION_TO_PG=true

# Nebo jeden connection string:
# DATABASE_URL=postgresql://postgres:tvoje_heslo@192.168.1.50:5432/fakturaApp
